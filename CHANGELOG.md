# Changelog

本文件记录每个正式版本的更新内容。发版前必须在 **`dev`** 为对应版本新增一节，且至少包含一条 `-` 开头的更新说明；`pnpm release:check --require-changelog` 与 Release CI 会校验。

格式：

```markdown
## [0.0.0] - 2026-01-01

### 新增
- 说明

### 修复
- 说明
```

## [0.4.3] - 2026-09-04

### 新增
- 入口 Key / 模型：ChatGPT 模型目录实时拉取；chip 选型；按 Key 查看用量；新建/导入弹窗；无 Key CTA；冲突时「去处理」
- 监控活动：展示入/出站端点与 token；支持查询、分页与删除记录
- 插件：安装包列表与开关、只读扫描源；显示 Pi packs 版本与指定版本状态
- 路由板：布局与端点提示优化；用量筛选对齐；支持自定义用量日期范围
- Agent logo 以圆角方图展示；环境页支持运行时安装/升级与更新检查
- 概览用量趋势可按 Agent 或模型切换；内嵌模型定价已刷新

### 变更
- 入口 Key 按类型分组；可删除池内各 Key（可选同步清理连接）
- ChatGPT / Claude / Grok 回退模型改为共享 JSON；界面样式与导航状态更清晰

### 修复
- 连接回收站：单条恢复不再连带其他项；同步当前登录忽略回收站账号
- 保存连接未改 Key 时保留原 API Key；透传请求正确记录 token；监控保留池上游端点
- 仅配置型 Claude/Grok pack 不再误判为已安装；无 Homebrew 时隐藏一键安装

## [0.4.2] - 2026-09-03

### 新增
- 连接页登录改为表格展示；行菜单支持连接池分享，清单可取消应用
- Agent 表格支持中英文本地化，过长内容自动截断
- 本机令牌支持命名、创建与删除入口 Key
- 项目可列出 Cursor 会话，并嵌套展示子 Agent；支持展开/收起
- Claude / Kimi 会话支持嵌套子 Agent
- 可复制单轮或整段会话记录
- 项目扫描结果写入 SQLite 缓存；Cursor 路径支持恢复与回退
- MCP 对重复路径使用唯一来源位置

### 变更
- Cursor 桌面应用列为额外副本，不再当作 CLI

### 修复
- 禁用 Cursor 授权管理入口

## [0.4.1] - 2026-09-03

### 新增
- 本机令牌可导入到匹配的 Agent / 连接
- 本机转发重启时显示黄条提示
- 冷启动加载与路由追踪日志更完整

### 变更
- 命名整改：本机转发、连接清单、路由页路径、技能同步等到内部零遗留（布局栏宽键硬切，升级后可能重置栏宽）
- 写入客户端与令牌页统一为「入口 Key」文案
- 保存入口 Key 时提升为默认池；非池行禁用改 Key

### 修复
- 重启后按本机设置纠正当前登录并提示
- e2e 本机转发筛选用量 aria 与文案一致

### 用量
- 刷新内置模型价格

## [0.4.0] - 2026-09-02

### 新增
- 侧栏默认显示路由；看板可开关本机转发，重启后记住开关状态
- 连接池：从连接页分享登录（所有 API Key；国产官方登录除外）、多选同步，也可在池里添加官方登录或 API Key
- 本机令牌：查看、复制、编辑入口密钥，一键测试，显示最近访问和用量；可写进对方认的登录
- 连接池支持自定义模型列表，并可从池里的登录刷新模型
- 连接页和连接池各有独立回收站
- 监控页按五段流程展示请求（本地端点、上游端点、转换过程），重启后仍保留近期记录
- Agents 详情统一安装、升级、卸载，可启动命令行或应用窗口
- Agent 列表使用本地标志图
- 侧栏支持右键展开/收起
- 备份可收回快照之后新建、且没改过的本机配置文件

### 变更
- 路由从默认隐藏改为侧栏可见（仍可在设置里关掉）

### 修复
- 空消息或缺少会话种子的请求会被本机转发拒绝，不再静默失败
- 连不上上游时提示上游不可用，不再误报连接池用尽
- Codex 在 Windows / macOS / Linux 上按系统打开应用窗口
- Kimi 未使用的安装渠道标为可选；旧版 Kimi 配置仍可被本机转发识别
- Linux 上 WorkBuddy / ZCode 的提示与可用操作更准确
- 监控详情里选中的请求与阶段保持一致；健康检查成功不再写入入站日志
- 当前登录不再被标成未使用
- 对本机转发不支持的 GET 会话请求，返回中英文说明，不再是空白的「方法不允许」

### 用量
- 刷新内置模型价格

## [0.3.11] - 2026-08-30

### 新增
- 设置里可以切换主色（紫色、蓝色、青色、玫红、橙色），按钮、开关和标志跟着变色
- Windows 桌面和开始菜单的快捷方式图标跟随主色重新着色
- 通知旁新增「反馈问题」入口，遇到问题或有建议时可以直接去填写
- 设置页按「语言与外观」「启动与关闭」「侧栏」「路由」「技能」「用量」分块显示
- Kimi Code 和 ZCode 的详情也列出三种对话接口，与其它 Agent 一致
- 用量页的筛选在本次使用中会记住，切换页面不用重新选
- 卸载确认框里的信息可以一键复制

### 变更
- 路由、插件和 MCP 标记为「开发中」；新安装默认不在侧栏显示，可以在设置里打开

### 修复
- 备份页左右分栏时布局不再错位
- 授权池里添加 Grok 官方登录时改为设备码（打开验证页并输入代码），与 Grok 自己的登录方式一致

### 用量
- 刷新内置模型价格

## [0.3.10] - 2026-08-30

### 新增
- Agents 详情列出对话接口类型、真实配置目录路径，渠道说法与列表一致
- 技能页支持项目技能：按工作区管理 `.agents/skills`
- 创建路由时重复密钥只提示、不拦截；同一 Agent 的同一地址会更新已有路由（设置里可开关）
- 连接页 API Key 登录显示供应商与端点
- ZCode 项目只读，删除请到 ZCode 里操作；用量从 ZCode 采集
- 登录、备份、MCP 等本地文件预览带语法高亮和折叠；点文件名可复制
- README 增加英文版和语言切换；界面预览改为实际窗口截图

### 修复
- 英文界面不再露出中文
- Windows 打开路径带空格的文件时不再找错目录
- WorkBuddy 模型目录改为追加，不覆盖其它条目
- 未安装的 Agent 也能打开详情；打开目录按钮统一为图标或「目录」
- 页面边距、标题栏、设置页对齐和路由页工具栏收紧

### 用量
- 刷新内置模型价格

## [0.3.9] - 2026-08-29

### 新增
- Agents 按同样方式管理 ZCode：添加 API Key 立刻写入模型列表；按目录追加，不覆盖其它条目
- WorkBuddy / ZCode 按自定义模型或供应商拆成多条登录；桌面套餐登录不导入；WorkBuddy 只写 `/v1/chat/completions`
- 接到 Codex / Grok 时按本机令牌走 Responses 口，不再根据请求正文猜测格式

### 修复
- WorkBuddy 写入时把 DeepSeek 官方 `/chat/completions` 补成 `/v1/chat/completions`
- 本机路由解绑不再卡住；Codex 官方登录改成 API Key 失败时会恢复原来的登录文件
- 对话流式输出更稳；请求头超时不再误切备用线路
- 打开会话不再反复折叠多 Agent 对话；连接页英文界面不再露出中文
- 对话消息加载失败会显示错误并可以重试，不再落到「开始对话」
- 官方登录出错时能看到对方返回的原因
- 带截图的较长请求不再被本机转发拦下
- 页面出错时给出提示，不再整页白屏
- 只有 ZCode 桌面、没有命令行时，不会拖垮其它 Agent 一起运行

## [0.3.8] - 2026-08-29

### 新增
- 连接页支持添加授权、导入授权；登录详情列出记下的配置文件，可复制并打开所在目录
- 登录按登录方式分行保存，记下关键词和整份配置；安全备份默认保留本机配置副本，可关闭自动堆积
- 登录详情补充套餐、有效期、时间线与完整端点地址
- 备份卡片改左右分栏，点开在右侧展示打码后的文件内容；密钥不以明文出现在列表或详情里

### 修复
- Codex 官方登录改用登记过的回调地址，授权页不再直接报错
- Grok 详情在没有用量时自动拉取，兼容只有周期没有百分比的账单
- Grok 换人时覆盖当前会话并写回官方槽名，导入的登录资料字段完整保留
- 自定义中转不再被显示成「官方」
- Windows 上子进程统一无窗启动，不再弹出 cmd 闪窗
- Claude 官方登录完成后可切换，等待倒计时对齐浏览器登录窗口
- 统一官方登录等待页：不再显示内部状态或登录文件路径，失败时重试为主按钮

### 用量
- 刷新内置模型价格

## [0.3.7] - 2026-08-28

### 修复
- 设置覆盖后，编辑页展示已解析的本机配置/登录文件路径
- WorkBuddy 原生安装走官网升级，不再误走应用内升级
- 勾选 Grok 官方时默认模型改为 grok-4.5，与内置预设一致
- 换模型按当前登录过滤，不再列出其他产品的目录
- 登录失效或模型不支持当前思考设置时记为失败，可点重试，不再显示「已完成」
- Pi 使用 grok-code-fast 时不再带思考参数；换模型走官方目录
- 回收站里已有身份的 Grok 登录显示末尾或主机；删除时把身份一并写入
- 编辑保存不会把打码的旧 Key 当成新密钥
- 刷新后客户端文件没写上、确认应用解绑或停止失败，都不再当成成功
- 复制本机路由地址的提示改为「地址已复制」
- 本机写入使用系统锁；解绑和创建路由失败可回滚

## [0.3.6] - 2026-08-28

### 修复
- 设置覆盖后，编辑页展示已解析的本机配置/登录文件路径
- WorkBuddy 原生安装走官网升级，不再误走应用内升级
- 勾选 Grok 官方时默认模型改为 grok-4.5，与内置预设一致
- 同一登录只保留一份正在用的来源；打开连接列表会把当前登录写回对应客户端
- Codex 官方登录不再被残留的 OpenRouter 设置抢走
- 连接向导「确认应用」不再把不完整的残留路由当成正在用的配置
- Kimi 自定义服务地址能走本机转发；不再把模型名改写成不存在的 kimi-k2
- Pi 切到 xAI 后设置会跟上当前供应商，不再沿用 OpenRouter 的残留模型
- Grok 不会把打码的旧 Key 当成正在用的 Key；回收站条目会带上密钥后四位和地址
- 上游返回 404 或缺少环境变量时记为失败，可用主按钮重试，不再显示成「已完成」

### 用量
- 刷新内置模型价格

## [0.3.5] - 2026-08-28

### 发版与 CI
- 统一发版流程：在 `dev` 升版并写 `CHANGELOG.md`，合入 `release` 后在 `dev` 打 tag
- `release` 分支守卫：只接受来自 `dev` 的合并
- 发版必须填写 `CHANGELOG.md`；GitHub Release 正文取自该文件
- 修复发版草稿尚未绑到版本号时，安装包无法上传到 GitHub Release
- 修复校验草稿时把「不是预发布」读成空，导致无法继续发版

### 功能与修复
- Agents 列表分栏打开详情；dev 线默认软隐藏 Cursor Agent
- DeepSeek 新建路由健康检查与单条记录展示
- 打开连接列表即合并重复登录；按来源写入客户端
- 修复对话、连接向导、本机转发、Grok 识别与本机令牌
- GLM 登录不再被误判成 Anthropic
- 命令行展示隐藏 `-p` 后的 prompt 内容

## [0.3.4] - 2026-08-27

### 发版与 CI
- 恢复 `release` 分支发版监控；tag 在 `dev` 打，合并到 `release` 后同一提交可触发 CI
- 新增 `release-branch-guard`：`release` 只接受来自 `dev` 的合并
- 发版必须填写本文件对应版本节；GitHub Release 正文取自此处

### 功能与修复
- Agents 列表支持分栏打开详情；dev 线默认软隐藏 Cursor Agent
- 修复 DeepSeek 新建路由健康检查与单条记录展示
- QA 批次：登录去重、Grok 回填、本机转发相关修复

## [0.3.3] - 2026-08-27

### 修复
- 固定 objc2 依赖到已发布的 0.3.2 lock 条目，修复 Linux 构建
- Release CI Linux job 固定到 ubuntu-24.04 runner

## [0.3.2] - 2026-08-26

### 修复
- Release CI 与 Linux 构建稳定性改进

## [0.3.1] - 2026-08-23

### 变更
- 早期 dest 集成线恢复版本
