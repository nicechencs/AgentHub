# Changelog

本文件记录每个正式版本的更新内容。发版前必须在 **`dev`** 为对应版本新增一节，且至少包含一条 `-` 开头的更新说明；`pnpm release:check --require-changelog` 与 Release CI 会校验。

格式：

```markdown
## [0.0.0] - 2026-01-01

### 新增
- 说明

### 修复
- 说明
```

## [0.3.7] - 2026-08-28

### 修复
- 设置覆盖后，编辑页展示已解析的本机配置/登录文件路径
- WorkBuddy 原生安装走官网升级，不再误走应用内升级
- 勾选 Grok 官方时默认模型改为 grok-4.5，与内置预设一致
- 换模型按当前登录过滤，不再列出其他产品的目录
- 登录失效或模型不支持当前思考设置时记为失败，可点重试，不再显示「已完成」
- Pi 使用 grok-code-fast 时不再带思考参数；换模型走官方目录
- 回收站里已有身份的 Grok 登录显示末尾或主机；删除时把身份一并写入
- 编辑保存不会把打码的旧 Key 当成新密钥
- 刷新后客户端文件没写上、确认应用解绑或停止失败，都不再当成成功
- 复制本机路由地址的提示改为「地址已复制」
- 本机写入使用系统锁；解绑和创建路由失败可回滚

## [0.3.6] - 2026-08-28

### 修复
- 设置覆盖后，编辑页展示已解析的本机配置/登录文件路径
- WorkBuddy 原生安装走官网升级，不再误走应用内升级
- 勾选 Grok 官方时默认模型改为 grok-4.5，与内置预设一致
- 同一登录只保留一份正在用的来源；打开连接列表会把当前登录写回对应客户端
- Codex 官方登录不再被残留的 OpenRouter 设置抢走
- 连接向导「确认应用」不再把不完整的残留路由当成正在用的配置
- Kimi 自定义服务地址能走本机转发；不再把模型名改写成不存在的 kimi-k2
- Pi 切到 xAI 后设置会跟上当前供应商，不再沿用 OpenRouter 的残留模型
- Grok 不会把打码的旧 Key 当成正在用的 Key；回收站条目会带上密钥后四位和地址
- 上游返回 404 或缺少环境变量时记为失败，可用主按钮重试，不再显示成「已完成」

### 用量
- 刷新内置模型价格

## [0.3.5] - 2026-08-28

### 发版与 CI
- 统一发版流程：在 `dev` 升版并写 `CHANGELOG.md`，合入 `release` 后在 `dev` 打 tag
- `release` 分支守卫：只接受来自 `dev` 的合并
- 发版必须填写 `CHANGELOG.md`；GitHub Release 正文取自该文件
- 修复发版草稿尚未绑到版本号时，安装包无法上传到 GitHub Release
- 修复校验草稿时把「不是预发布」读成空，导致无法继续发版

### 功能与修复
- Agents 列表分栏打开详情；dev 线默认软隐藏 Cursor Agent
- DeepSeek 新建路由健康检查与单条记录展示
- 打开连接列表即合并重复登录；按来源写入客户端
- 修复对话、连接向导、本机转发、Grok 识别与本机令牌
- GLM 登录不再被误判成 Anthropic
- 命令行展示隐藏 `-p` 后的 prompt 内容

## [0.3.4] - 2026-08-27

### 发版与 CI
- 恢复 `release` 分支发版监控；tag 在 `dev` 打，合并到 `release` 后同一提交可触发 CI
- 新增 `release-branch-guard`：`release` 只接受来自 `dev` 的合并
- 发版必须填写本文件对应版本节；GitHub Release 正文取自此处

### 功能与修复
- Agents 列表支持分栏打开详情；dev 线默认软隐藏 Cursor Agent
- 修复 DeepSeek 新建路由健康检查与单条记录展示
- QA 批次：登录去重、Grok 回填、本机转发相关修复

## [0.3.3] - 2026-08-27

### 修复
- 固定 objc2 依赖到已发布的 0.3.2 lock 条目，修复 Linux 构建
- Release CI Linux job 固定到 ubuntu-24.04 runner

## [0.3.2] - 2026-08-26

### 修复
- Release CI 与 Linux 构建稳定性改进

## [0.3.1] - 2026-08-23

### 变更
- 早期 dest 集成线恢复版本
